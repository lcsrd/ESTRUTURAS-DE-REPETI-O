#include <stdio.h>

int main(void)
{
    int contador; //variável de controle do loop
    
    for(contador = 1; contador <= 500; contador++)
    {
        printf("%d ", contador);
    }
    printf("\n");
  
  return(0);
}
